if (typeof define !== 'function') {
    var define = require('amdefine')(module);
}
define( function(require,exports,module) {
    const BaseView           = require('core_plugins/base_plugin/views/base_view').View;
    const QualityCheckerView = require('./quality_checker_view').View;

    class QualityCheckerMenuView extends BaseView {
        constructor(params) {
            super(params);
            this.initializeElements();
            this.qualityCheckerView = new QualityCheckerView(params);
            this.qualityCheckerView.preRendering();
            super.render();
        }
    };

    module.exports.View = QualityCheckerMenuView;

});