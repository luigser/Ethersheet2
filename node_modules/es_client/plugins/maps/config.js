if (typeof define !== 'function') { var define = require('amdefine')(module) }
define( function(require){
    return {
       NAME            : "maps",
       VIEW            : "map_menu",
       PLACEHOLDER     : null,
       ELEMENT_TO_FILL : "#es-modal-box"
    };
});