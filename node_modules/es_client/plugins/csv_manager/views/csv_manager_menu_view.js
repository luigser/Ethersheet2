if (typeof define !== 'function') {
    var define = require('amdefine')(module);
}
define( function(require,exports,module) {
    const BaseView         = require('core_plugins/base_plugin/views/base_view').View;
    const CsvManagerView   = require('./csv_manager_view').View;

    class CsvManagerMenuView extends BaseView {
        constructor(params) {
            super(params);
            this.initializeElements();
            this.csvManagerView = new CsvManagerView(params);
            this.csvManagerView.preRendering();
            super.render();
        }
    };

    module.exports.View = CsvManagerMenuView;

});