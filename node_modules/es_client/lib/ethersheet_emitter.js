if (typeof define !== 'function') { var define = require('amdefine')(module) }
define( function(require,exports,module) {

    const EventEmitter = require('./event_emitter').EventEmitter;

    module.exports.EthersheetEvents = {
      InitializationEvent : "InitializationEvent",
      CreteViewEvent      : "CreateViewEvent"
    };

    class EthersheetEmitter extends EventEmitter
    {
        constructor(){
            super();
        }
    };

    module.exports.EthersheetEmitter = EthersheetEmitter;

});