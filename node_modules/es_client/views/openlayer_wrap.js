if (typeof define !== 'function') { var define = require('amdefine')(module) }

define(
    "OpenlayerWrapper",
    ['ol'],
    function (ol){

       function OpenlayerWrapper (){
            Geocoder = require('ol3-geocoder');
       }

        OpenlayerWrapper.prototype = new Geocoder();

       return OpenlayerWrapper;
    }
);
