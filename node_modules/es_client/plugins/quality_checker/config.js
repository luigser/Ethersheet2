if (typeof define !== 'function') { var define = require('amdefine')(module) }
define( function(require){
    return {
        NAME        : "quality_checker",
        VIEW        : "quality_checker_menu_view",
        PLACEHOLDER : "es_header",
        ELEMENT     : "#es-header"
    };
});