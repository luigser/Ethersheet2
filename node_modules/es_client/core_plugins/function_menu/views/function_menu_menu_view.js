if (typeof define !== 'function') {
    var define = require('amdefine')(module);
}
define( function(require,exports,module) {
    const BaseView           = require('core_plugins/base_plugin/views/base_view').View;
    const FunctionMenuView = require('./function_menu_view').View;

    class FunctionMenuMenuView extends BaseView {
        constructor(params) {
            super(params);
            this.initializeElements();
            this.functionMenuView = new FunctionMenuView(params);
            this.events = {
                'click #es-function-icon' : 'onMenuContentRendering'
            };
            super.render();
        }

        onMenuContentRendering() {
            this.functionMenuView.preRendering();
        }
    };

    module.exports.View = FunctionMenuMenuView;

});